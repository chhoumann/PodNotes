import { builtinModules } from "node:module";
import path from "node:path";
import { fileURLToPath } from "node:url";
import { svelte } from "@sveltejs/vite-plugin-svelte";
import { defineConfig } from "vite";
import { createBuildArtifactPlugin } from "./scripts/build-artifacts.ts";

const banner = `/*
THIS IS A GENERATED/BUNDLED FILE BY VITE
If you want to view the source, please visit the GitHub repository of this plugin.
*/
`;

const rootDir = path.dirname(fileURLToPath(import.meta.url));

const externalDeps = [
	"obsidian",
	"electron",
	"@codemirror/autocomplete",
	"@codemirror/closebrackets",
	"@codemirror/collab",
	"@codemirror/commands",
	"@codemirror/comment",
	"@codemirror/fold",
	"@codemirror/gutter",
	"@codemirror/highlight",
	"@codemirror/history",
	"@codemirror/language",
	"@codemirror/lint",
	"@codemirror/matchbrackets",
	"@codemirror/panel",
	"@codemirror/rangeset",
	"@codemirror/rectangular-selection",
	"@codemirror/search",
	"@codemirror/state",
	"@codemirror/stream-parser",
	"@codemirror/text",
	"@codemirror/tooltip",
	"@codemirror/view",
	"@lezer/common",
	"@lezer/highlight",
	"@lezer/lr",
];

const builtinExternals = builtinModules.filter((moduleName) => !moduleName.startsWith("_"));

const external = [
	...externalDeps,
	...builtinExternals,
	...builtinExternals.map((moduleName) => `node:${moduleName}`),
];

export default defineConfig(({ mode }) => {
	const isProd = mode === "production";
	const outDir = path.resolve(rootDir, "build");

	return {
		plugins: [
			svelte({
				compilerOptions: { css: "injected" },
			}),
			createBuildArtifactPlugin({ isProduction: isProd, outDir, rootDir }),
		],
		resolve: {
			alias: {
				src: path.resolve(rootDir, "src"),
			},
			conditions: ["browser"],
		},
		build: {
			lib: {
				entry: path.resolve(rootDir, "src/main.ts"),
				formats: ["cjs"],
				fileName: () => "main.js",
			},
			target: "es2020",
			outDir,
			emptyOutDir: true,
			sourcemap: !isProd,
			minify: false,
			rollupOptions: {
				external,
				output: {
					entryFileNames: "main.js",
					codeSplitting: false,
					exports: "auto",
					banner,
					// Vite 8 dropped the esbuild transform (and with it
					// esbuild.drop), so prod minification and console
					// stripping both move to rolldown's oxc minifier.
					minify: isProd
						? { compress: { dropConsole: true }, mangle: true, codegen: true }
						: false,
				},
			},
		},
	};
});
